# 安全与隐私

## 安全边界

Jarvis 只向模型开放显式定义的工具。打开应用会查找已安装快捷方式或少量预定义的系统程序，网页 URL 仅允许 `http` 与 `https`。模型不能直接运行任意命令行。

这些限制尚未经过独立安全审计；请不要用当前 Alpha 版本处理高风险自动化任务。联网查询返回的网页或比分内容属于不可信数据，不应被当作用户指令。

## 本地数据

`data/jarvis_memory.db` 包含对话、提醒、工具记录和窗口标题；`voice/current_input.wav` 是临时录音。两者都不应提交到 GitHub。公开 bug 报告前，请移除日志和截图中的个人信息。

## 报告漏洞

请优先使用仓库的 GitHub 私密漏洞报告功能；如果该功能不可用，请先开一个不包含利用细节的 issue 请求私密联系渠道。不要在公开 issue 中粘贴令牌、个人数据或可直接复现的攻击载荷。
